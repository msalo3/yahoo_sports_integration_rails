<p id="notice"><%= notice %></p>

<h1>Connection to Yahoo</h1>

<% if @connections.length > 0 %>
  <table>
    <thead>
      <tr>
        <th>Connection Status</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @connections.each do |connection| %>
        <tr>
          <% if connection.updated_at > Time.now - 60.minutes %>
            <td> Current Connection is live! </td>
          <% else %>
            <td> Current Connection is not live. Please edit and reconnect! </td>
          <% end %>
        </tr>
        <tr>
          <td><%= link_to 'Show', connection %></td>
          <td><%= link_to 'Edit', edit_connection_path(connection) %></td>
          <td><%= link_to 'Destroy', connection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <td><%= link_to 'Create a Connection', new_connection_path %></td>
<% end %>

<br>
